using System;
using System.Collections.Generic;
using System.Linq;
using System.Threading.Tasks;

namespace CodeChallenge
{
    public class SolutionFour
    {
        static int numberfour()
        {
            int n = 0;
            int [] arr[n] = {4, 6, 4, 5, 6, 2};
            for(int i = 0; i < arr[n].Length, i++)
            if(n[0] < n[1])
            {
                n[1] = n[0] +1;
            }  
        return n;
        }
    }
}